import ContactForm from "@/components/ui/ContactForm";

export const metadata = {
  title: "Contact Us",
  description:
    "Get in touch with the AniArix team — questions, requests, or feedback.",
};

export default function ContactPage() {
  return <ContactForm />;
}